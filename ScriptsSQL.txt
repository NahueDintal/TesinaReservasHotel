//Creacion de tablas
-- Tabla: DocumentType
CREATE TABLE DocumentType (
    idDocumentType INT AUTO_INCREMENT,
    name VARCHAR(100) NOT NULL,
    PRIMARY KEY (idDocumentType)
);
-- Tabla: Country
CREATE TABLE Country (
    idCountry INT AUTO_INCREMENT,
    name VARCHAR(100) NOT NULL,
    PRIMARY KEY (idCountry)
);
-- Tabla: CustomerStatus
CREATE TABLE CustomerStatus (
    idCustomerStatus INT AUTO_INCREMENT,
    name VARCHAR(100) NOT NULL,
    PRIMARY KEY (idCustomerStatus)
);
-- Tabla: CustomerOrigin
CREATE TABLE CustomerOrigin (
    idCustomerOrigin INT AUTO_INCREMENT,
    name VARCHAR(100) NOT NULL,
    PRIMARY KEY (idCustomerOrigin)
);
-- Tabla: Customer
CREATE TABLE Customer (
    idCustomer INT AUTO_INCREMENT,
    name VARCHAR(100) NOT NULL,
    surname VARCHAR(100) NOT NULL,
    idDocumentType INT NOT NULL,
    documentNumber VARCHAR(30) NOT NULL,
    phoneNumber VARCHAR(30),
    email VARCHAR(255),
    idCountry INT NOT NULL,
    idCustomerStatus INT NOT NULL,
    idCustomerOrigin INT NOT NULL,
    PRIMARY KEY (idCustomer),
    FOREIGN KEY (idDocumentType)
        REFERENCES DocumentType(idDocumentType),
    FOREIGN KEY (idCountry)
        REFERENCES Country(idCountry),
    FOREIGN KEY (idCustomerStatus)
        REFERENCES CustomerStatus(idCustomerStatus),
    FOREIGN KEY (idCustomerOrigin)
        REFERENCES CustomerOrigin(idCustomerOrigin)
);


-- ==========================================
-- Tabla: ReservationStatus
-- ==========================================
CREATE TABLE `reservationstatus` (
    `idReservationStatus` int NOT NULL AUTO_INCREMENT,
    `name` varchar(30) NOT NULL,
    PRIMARY KEY (`idReservationStatus`)
);

-- ==========================================
-- Tabla: ReservationType
-- ==========================================

CREATE TABLE ReservationType (
    idReservationType INT AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(50) NOT NULL
);

-- ==========================================
-- Tabla: Reservation
-- ==========================================

CREATE TABLE Reservation (
    idReservation INT AUTO_INCREMENT PRIMARY KEY,
    idCustomer INT NOT NULL,
    creationDate DATETIME NOT NULL,
    checkIn DATE NOT NULL,
    checkOut DATE NOT NULL,
    idReservationStatus INT NOT NULL,
    idReservationType INT NOT NULL,
    numberOfGuests INT NOT NULL,
    totalRate DECIMAL(10,2) NOT NULL,
    observations VARCHAR(500),

    CONSTRAINT fk_reservation_customer
        FOREIGN KEY (idCustomer)
        REFERENCES Customer(idCustomer),

    CONSTRAINT fk_reservation_status
        FOREIGN KEY (idReservationStatus)
        REFERENCES ReservationStatus(idReservationStatus),

    CONSTRAINT fk_reservation_type
        FOREIGN KEY (idReservationType)
        REFERENCES ReservationType(idReservationType)
);

-- ==========================================
-- Tabla: PaymentMethod
-- ==========================================

CREATE TABLE `paymentmethod` (
    `idPaymentMethod` int NOT NULL AUTO_INCREMENT,
    `name` varchar(50) NOT NULL,
    PRIMARY KEY (`idPaymentMethod`)
);

-- ==========================================
-- Tabla: PaymentStatus
-- ==========================================

CREATE TABLE `paymentstatus` (
    `idPaymentStatus` int NOT NULL AUTO_INCREMENT,
    `name` varchar(50) NOT NULL,
    PRIMARY KEY (`idPaymentStatus`)
);

-- ==========================================
-- Tabla: Payment
-- ==========================================

CREATE TABLE `payment` (
    `idPayment` int NOT NULL AUTO_INCREMENT,
    `idReservation` int NOT NULL,
    `amount` decimal(10,2) NOT NULL,
    `paymentDate` datetime NOT NULL,
    `idPaymentMethod` int NOT NULL,
    `idPaymentStatus` int NOT NULL,
    `observations` text,

    PRIMARY KEY (`idPayment`),

    KEY `idReservation` (`idReservation`),
    KEY `idPaymentMethod` (`idPaymentMethod`),
    KEY `idPaymentStatus` (`idPaymentStatus`),

    CONSTRAINT `payment_ibfk_1`
        FOREIGN KEY (`idReservation`)
        REFERENCES `reservation` (`idReservation`),

    CONSTRAINT `payment_ibfk_2`
        FOREIGN KEY (`idPaymentMethod`)
        REFERENCES `paymentmethod` (`idPaymentMethod`),

    CONSTRAINT `payment_ibfk_3`
        FOREIGN KEY (`idPaymentStatus`)
        REFERENCES `paymentstatus` (`idPaymentStatus`)
);

-- ==========================================
-- Tabla: Service
-- ==========================================

CREATE TABLE Service (
    idService INT AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(100) NOT NULL,
    description VARCHAR(255),
    price DECIMAL(10,2) NOT NULL,
    active BOOLEAN NOT NULL DEFAULT TRUE
);

-- ==========================================
-- Tabla: Product
-- ==========================================

CREATE TABLE Product (
    idProduct INT AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(100) NOT NULL,
    description VARCHAR(255),
    price DECIMAL(10,2) NOT NULL,
    active BOOLEAN NOT NULL DEFAULT TRUE
);

-- ==========================================
-- Tabla: ConsumptionType
-- ==========================================

CREATE TABLE ConsumptionType (
    idConsumptionType INT AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(100) NOT NULL
);

-- ==========================================
-- Tabla: Consumption
-- ==========================================

CREATE TABLE Consumption (
    idConsumption INT AUTO_INCREMENT PRIMARY KEY,

    idReservation INT NOT NULL,
    idConsumptionType INT NOT NULL,

    idProduct INT NULL,
    idService INT NULL,

    quantity INT NOT NULL,
    unitPrice DECIMAL(10,2) NOT NULL,
    total DECIMAL(10,2) NOT NULL,

    consumptionDate DATETIME NOT NULL,

    idPaymentStatus INT NOT NULL,

    observations VARCHAR(500) NULL,

    CONSTRAINT fk_consumption_reservation
        FOREIGN KEY (idReservation)
        REFERENCES Reservation(idReservation),

    CONSTRAINT fk_consumption_type
        FOREIGN KEY (idConsumptionType)
        REFERENCES ConsumptionType(idConsumptionType),

    CONSTRAINT fk_consumption_product
        FOREIGN KEY (idProduct)
        REFERENCES Product(idProduct),

    CONSTRAINT fk_consumption_service
        FOREIGN KEY (idService)
        REFERENCES Service(idService),

    CONSTRAINT fk_consumption_payment_status
        FOREIGN KEY (idPaymentStatus)
        REFERENCES PaymentStatus(idPaymentStatus)
);

CREATE TABLE IF NOT EXISTS room (
    number INT PRIMARY KEY,
    floor INT NOT NULL,
    type VARCHAR(20) NOT NULL,
    capacity INT NOT NULL,
    view VARCHAR(50),
    available TINYINT(1) NOT NULL DEFAULT 1,
    out_of_service TINYINT(1) NOT NULL DEFAULT 0,
    features VARCHAR(255),
    price DECIMAL(10,2) NOT NULL,
    description TEXT
);







//carga de datos
-- Tabla: DocumentType
INSERT INTO DocumentType (name) VALUES
('dni'),
('pasaporte'),
('cedula de identidad'),
('pasaporte');

INSERT INTO Country (name) VALUES
('afganistan'), ('albania'), ('alemania'), ('andorra'), ('angola'),
('antigua y barbuda'), ('arabia saudita'), ('argelia'), ('argentina'), ('armenia'),
('australia'), ('austria'), ('azerbaiyan'), ('bahamas'), ('banglades'),
('barbados'), ('barein'), ('belgica'), ('belice'), ('benin'),
('bielorrusia'), ('birmania myanmar'), ('bolivia'), ('bosnia y herzegovina'), ('botsuana'),
('brasil'), ('brunei'), ('bulgaria'), ('burkina faso'), ('burundi'),
('butan'), ('cabo verde'), ('camboya'), ('camerun'), ('canada'),
('catar'), ('chad'), ('chile'), ('china'), ('chipre'),
('ciudad del vaticano'), ('colombia'), ('comoras'), ('corea del norte'), ('corea del sur'),
('costa de marfil'), ('costa rica'), ('croacia'), ('cuba'), ('dinamarca'),
('dominica'), ('ecuador'), ('egipto'), ('el salvador'), ('emiratos arabes unidos'),
('eritrea'), ('eslovaquia'), ('eslovenia'), ('españa'), ('estados unidos'),
('estonia'), ('eswatini suazilandia'), ('etiopia'), ('filipinas'), ('finlandia'),
('fiyi'), ('francia'), ('gabon'), ('gambia'), ('georgia'),
('ghana'), ('granada'), ('grecia'), ('guatemala'), ('guyana'),
('guinea'), ('guinea bissau'), ('guinea ecuatorial'), ('haiti'), ('honduras'),
('hungria'), ('india'), ('indonesia'), ('irak'), ('iran'),
('irlanda'), ('islandia'), ('islas marshall'), ('islas salomon'), ('israel'),
('italia'), ('jamaica'), ('japon'), ('jordania'), ('kazajistan'),
('kenia'), ('kirguistan'), ('kiribati'), ('kuwait'), ('laos'),
('lesoto'), ('letonia'), ('libano'), ('liberia'), ('libia'),
('liechtenstein'), ('lituania'), ('luxemburgo'), ('macedonia del norte'), ('madagascar'),
('malasia'), ('malaui');

-- Tabla: CustomerStatus
INSERT INTO CustomerStatus (name) VALUES
('active'),
('inactive');

-- Tabla: CustomerOrigin
INSERT INTO CustomerOrigin (name) VALUES
('airbnb'),
('booking'),
('particular'),

('walk-in');

('walk-in');

